apply plugin: 'groovy'

dependencies {
    //gradle sdk
    //groovy sdk
    compile 'com.android.tools.build:gradle:2.1.0'
    compile 'org.javassist:javassist:3.20.0-GA'
}

repositories {
    jcenter()
    maven { url "http://repo.caimi-inc.com/nexus/content/repositories/releases/" }
}